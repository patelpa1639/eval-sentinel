[pytest]
# Make `import src.*` work without installing the package.
pythonpath = .
testpaths = tests
markers =
    slow: live/long-running tests (agent loops, network); excluded by default
# Default run excludes slow tests; `make test-all` overrides this.
addopts = -m "not slow"
asyncio_mode = auto
